# Frescapp

This is an app created with [`create-expo-app`](https://www.npmjs.com/package/create-expo-app) called Frescapp, in which the interprice will sell products through the app. 
## Get started

1. Install dependencies

   ```bash
   npm install
   ```

2. Start the app

   ```bash
   npx expo start
   ```
